# Set to true to use the Express server (server/ folder, port 3002)
# Set to false (or omit) to use the in-browser MockApiService (localStorage)
VITE_USE_SERVER=true

# Base URL of the Express server (only used when VITE_USE_SERVER=true)
VITE_API_URL=http://localhost:3002

